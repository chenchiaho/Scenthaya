/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['en'],
        defaultLocale: 'en',
    },
    images: {
        // domains: ['strapi-be.dev.wonderland.tw', 'localhost', '127.0.0.1'],
    },
}

module.exports = nextConfig
